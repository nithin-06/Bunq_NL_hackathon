# Round — Bill Splitter
# Install: pip install -r requirements.txt

# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# OCR
# Pin the legacy PaddleOCR 2.x stack. The receipt parser in this repo
# expects the 2.x API and result shape, and breaks on PaddleOCR 3.x.
numpy==1.26.4
opencv-python==4.6.0.66
protobuf==3.20.2
paddlepaddle==2.6.2
paddleocr==2.7.0.3
rapidfuzz>=3.0.0
Pillow>=10.0.0

# Voice
openai-whisper==20240930
# Pin Torch to a stable Windows/Python 3.10 CPU wheel for Whisper.
torch==2.5.1
sounddevice
scipy

# bunq auth
cryptography>=41.0.0
requests>=2.31.0

# LLM (Ollama is external — run: ollama serve)
# No pip install needed for Ollama

# Anthropic API (for Claude vision OCR)
anthropic>=0.25.0